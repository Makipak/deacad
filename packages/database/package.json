{
  "name": "@deacad/database",
  "private": true,
  "version": "0.1.0",
  "description": "Prisma schema, migration, seed, dan client singleton — satu-satunya cara akses database",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "pg": "^8.13.1",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@deacad/config": "workspace:*",
    "dotenv": "^17.4.1",
    "prisma": "^7.3.0",
    "tsx": "^4.19.2",
    "typescript": "catalog:",
    "@types/pg": "^8.11.10",
    "@types/bcrypt": "^5.0.2"
  }
}
